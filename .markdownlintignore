# Generated files. release-please owns CHANGELOG.md and writes it in its own
# style; docs/api is TypeDoc output. Linting either one asks a generator to
# satisfy rules written for hand-authored prose, which it will never do and
# which would block every release.
CHANGELOG.md
docs/api/
node_modules/
dist/

# Untracked and gitignored, so CI never sees it — but a local lint run globs the
# working tree rather than the index, and would fail on drafts and scratch notes
# that are nobody's business but the maintainer's.
plinth/private/
