/**
 * astrologyjs — public API surface.
 *
 * This is the package's only barrel file. Internal modules import from each
 * other directly; barrels are banned everywhere else (see
 * `plinth/coding-standards-typescript.md` §Modules).
 */

export const VERSION = '2.0.0-dev';
